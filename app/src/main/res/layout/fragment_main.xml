<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivityFragment"
    android:orientation="vertical"
    android:gravity="center">

    <!-- Label -->
    <TextView
        android:layout_gravity="center"
        android:id="@+id/portfolio_apps_label"
        android:textSize="24sp"
        android:text="@string/portfolio_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingBottom="18dp"/>

    <!-- The list of launch app buttons -->
    <ListView
        android:layout_alignTop="@id/portfolio_apps_label"
        android:id="@+id/portfolio_apps_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:divider="@android:color/transparent"
        android:minHeight="?attr/listPreferredItemHeightLarge"
        tools:listitem="@layout/apps_list_item"/>

</LinearLayout>
